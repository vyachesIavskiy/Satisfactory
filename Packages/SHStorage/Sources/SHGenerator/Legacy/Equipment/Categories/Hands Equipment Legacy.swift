import SHModels
import SHStaticModels

extension Legacy.Equipment {
    static let portableMiner = Equipment.Static.Legacy(id: "portable-miner")
    static let beacon = Equipment.Static.Legacy(id: "beacon")
}
