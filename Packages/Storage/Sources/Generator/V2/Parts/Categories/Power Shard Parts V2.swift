import StaticModels

extension V2.Parts {
    static let powerShard = Part(id: "part-power-shard", category: .powerShards, form: .solid)
    
    static let powerShardParts = [
        powerShard
    ]
}
