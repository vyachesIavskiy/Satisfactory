let fence1 = Building(name: "Fence 1", buildingType: .attachments)
let stairsRight = Building(name: "Stairs Right", buildingType: .attachments)
let stairsLeft = Building(name: "Stairs Left", buildingType: .attachments)

let Attachments = [
    fence1,
    stairsRight,
    stairsLeft
]
