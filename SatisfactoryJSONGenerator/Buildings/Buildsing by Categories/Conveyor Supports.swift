let conveyorPole = Building(name: "Conveyor Pole", buildingType: .conveyorSupports)
let stackableConveyorPole = Building(name: "Stackable Conveyor Pole", buildingType: .conveyorSupports)
let conveyorWallMount = Building(name: "Conveyor Wall Mount", buildingType: .conveyorSupports)

let ConveyorSupports = [
    conveyorPole,
    stackableConveyorPole,
    conveyorWallMount
]
