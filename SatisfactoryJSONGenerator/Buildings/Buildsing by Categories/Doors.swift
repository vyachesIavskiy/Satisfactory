let leftDoorWall = Building(name: "Left Door Wall", buildingType: .doors)
let centerDoorWall = Building(name: "Center Door Wall", buildingType: .doors)
let rightDoorWall = Building(name: "Right Door Wall", buildingType: .doors)
let leftDoorWallGray = Building(name: "Left Door Wall (Gray)", buildingType: .doors)
let centerDoorWallGray = Building(name: "Center Door Wall (Gray)", buildingType: .doors)
let rightDoorWallGray = Building(name: "Right Door Wall (Gray)", buildingType: .doors)
let gateWall = Building(name: "Gate Wall", buildingType: .doors)

let Doors = [
    leftDoorWall,
    centerDoorWall,
    rightDoorWall,
    leftDoorWallGray,
    centerDoorWallGray,
    rightDoorWallGray,
    gateWall
]
