let biomassGenerator = Building(name: "Biomass Generator", buildingType: .generators)
let coalGenerator = Building(name: "Coal Generator", buildingType: .generators)
let fuelGenerator = Building(name: "Fuel Generator", buildingType: .generators)
let nuclearPowerGenerator = Building(name: "Nuclear Power Generator", buildingType: .generators)
let geothermalGenerator = Building(name: "Geothermal Generator", buildingType: .generators)

let Generators = [
    biomassGenerator,
    coalGenerator,
    fuelGenerator,
    nuclearPowerGenerator,
    geothermalGenerator
]
