let trainStation = Building(name: "Train Station", buildingType: .railedVehicles)
let freightPlatform = Building(name: "Freight Platform", buildingType: .railedVehicles)
let fluidFreightPlatform = Building(name: "Fluid Freight Platform", buildingType: .railedVehicles)
let emptyPlatform = Building(name: "Empty Platform", buildingType: .railedVehicles)
let railway = Building(name: "Railway", buildingType: .railedVehicles)

let RailedVehicles = [
    trainStation,
    freightPlatform,
    fluidFreightPlatform,
    emptyPlatform,
    railway
]
