let coveyorSplitter = Building(name: "Conveyor Splitter", buildingType: .sorting)
let smartSplitter = Building(name: "Smart Splitter", buildingType: .sorting)
let programmableSplitter = Building(name: "Programmable Splitter", buildingType: .sorting)
let conveyorMerger = Building(name: "Conveyor Merger", buildingType: .sorting)

let Sorting = [
    coveyorSplitter,
    smartSplitter,
    programmableSplitter,
    conveyorMerger
]
