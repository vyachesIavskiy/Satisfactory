let personalStorageBox = Building(name: "Personal Storage Box", buildingType: .storage)
let storageContainer = Building(name: "Storage Container", buildingType: .storage)
let industrialStorageContainer = Building(name: "Industrial Storage Container", buildingType: .storage)
let fluidBuffer = Building(name: "Fluid Buffer", buildingType: .storage)
let industrialFluidBuffer = Building(name: "Industrial Fluid Buffer", buildingType: .storage)

let Storage = [
    personalStorageBox,
    storageContainer,
    industrialStorageContainer,
    fluidBuffer,
    industrialFluidBuffer
]
