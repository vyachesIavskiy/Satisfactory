let reinforcedWindow = Building(name: "Reinforced Window", buildingType: .windows)
let frameWindow = Building(name: "Frame Window", buildingType: .windows)
let singleWindow = Building(name: "Single Window", buildingType: .windows)
let pannelWindow = Building(name: "Pannel Window", buildingType: .windows)

let Windows = [
    reinforcedWindow,
    frameWindow,
    singleWindow,
    pannelWindow
]
