let emptyCanister = Part(name: "Empty Canister", partType: .containers)
let emptyFluidTank = Part(name: "Empty Fluid Tank", partType: .containers)
let pressureConversionCube = Part(name: "Pressure Conversion Cube", partType: .containers)
let packagedWater = Part(name: "Packaged Water", partType: .containers)
let packagedOil = Part(name: "Packaged Oil", partType: .containers)
let packagedFuel = Part(name: "Packaged Fuel", partType: .containers)
let packagedHeavyOilResidue = Part(name: "Packaged Heavy Oil Residue", partType: .containers)
let packagedLiquidBiofuel = Part(name: "Packaged Liquid Biofuel", partType: .containers)
let packagedTurbofuel = Part(name: "Packaged Turbofuel", partType: .containers)
let packagedAluminaSolution = Part(name: "Packaged Alumina Solution", partType: .containers)
let packagedSulfuricAcid = Part(name: "Packaged Sulfuric Acid", partType: .containers)
let packagedNitricAcid = Part(name: "Packaged Nitric Acid", partType: .containers)
let packagedNitrogenGas = Part(name: "Packaged Nitrogen Gas", partType: .containers)

let Containers = [
    emptyCanister,
    emptyFluidTank,
    pressureConversionCube,
    packagedWater,
    packagedOil,
    packagedFuel,
    packagedHeavyOilResidue,
    packagedLiquidBiofuel,
    packagedTurbofuel,
    packagedAluminaSolution,
    packagedSulfuricAcid,
    packagedNitricAcid,
    packagedNitrogenGas
]
