let wire = Part(name: "Wire", partType: .electronics)
let cable = Part(name: "Cable", partType: .electronics)
let battery = Part(name: "Battery", partType: .electronics)
let quickwire = Part(name: "Quickwire", partType: .electronics)
let aiLimiter = Part(name: "A.I. Limiter", partType: .electronics)
let circuitBoard = Part(name: "Circuit Board", partType: .electronics)
let highSpeedConnector = Part(name: "High-Speed Connector", partType: .electronics)

let Electronics = [
    wire,
    cable,
    battery,
    quickwire,
    aiLimiter,
    circuitBoard,
    highSpeedConnector
]
