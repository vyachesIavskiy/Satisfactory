let rotor = Part(name: "Rotor", partType: .industrialParts)
let stator = Part(name: "Stator", partType: .industrialParts)
let motor = Part(name: "Motor", partType: .industrialParts)
let heatSink = Part(name: "Heat Sink", partType: .industrialParts)
let coolingSystem = Part(name: "Cooling System", partType: .industrialParts)
let fusedModularFrame = Part(name: "Fused Modular Frame", partType: .industrialParts)
let battery = Part(name: "Battery", partType: .electronics)
let turboMotor = Part(name: "Turbo Motor", partType: .industrialParts)

let IndustrialParts = [
    rotor,
    stator,
    motor,
    heatSink,
    coolingSystem,
    fusedModularFrame,
    battery,
    turboMotor
]
