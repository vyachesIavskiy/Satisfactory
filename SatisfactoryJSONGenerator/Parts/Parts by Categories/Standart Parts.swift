let ironPlate = Part(name: "Iron Plate", partType: .standartParts)
let ironRod = Part(name: "Iron Rod", partType: .standartParts)
let screw = Part(name: "Screw", partType: .standartParts)
let reinforcedIronPlate = Part(name: "Reinforced Iron Plate", partType: .standartParts)
let copperSheet = Part(name: "Copper Sheet", partType: .standartParts)
let modularFrame = Part(name: "Modular Frame", partType: .standartParts)
let steelBeam = Part(name: "Steel Beam", partType: .standartParts)
let steelPipe = Part(name: "Steel Pipe", partType: .standartParts)
let encasedIndustrialBeam = Part(name: "Encased Industrial Beam", partType: .standartParts)
let heavyModularFrame = Part(name: "Heavy Modular Frame", partType: .standartParts)
let alcladAluminumSheet = Part(name: "Alclad Aluminum Sheet", partType: .standartParts)

let StandartParts = [
    ironPlate,
    ironRod,
    screw,
    reinforcedIronPlate,
    copperSheet,
    modularFrame,
    steelBeam,
    steelPipe,
    encasedIndustrialBeam,
    heavyModularFrame,
    alcladAluminumSheet
]
